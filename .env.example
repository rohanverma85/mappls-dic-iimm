# Domain-whitelisted browser token used by Mappls Web Maps SDK v3.0 and server-side reverse geocoding.
# Leave blank to use the built-in operational GIS preview without a commercial basemap.
MAPPLS_ACCESS_TOKEN=

# Replit and production environments provide PORT automatically.
PORT=3000
